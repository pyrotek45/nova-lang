module bench_03_string_concat
// Benchmark 3: String concatenation stress
// Builds a large string by repeated concatenation.
// Each concat allocates a new heap string object; the old ones become garbage.

let s = ""
for i in 0..10000 {
    s = s + "x"
}
