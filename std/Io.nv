struct Io {
    println : (any),
    prompt : (str) -> str,
}

module Io {
    println = fn(input:any) {
        print(input)
        print("\n")
    },
    prompt = fn(input:str) -> str {
        print(input + "\n")
        return readline()
    }
}