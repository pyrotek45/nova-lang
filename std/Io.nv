struct Io {
    println : (Any),
    prompt : (String) -> String,
}

module Io {
    println = fn(input: Any) {
        print(input)
        print("\n")
    },
    prompt = fn(input: String) -> String {
        print(input + "\n")
        return readline()
    }
}