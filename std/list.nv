fn fill(list: [$A], input: $A, ammount: Int) -> [$A] {
    for i <- 0; i < ammount; i += 1 {
        list.push(input)
    }
    return list
}

fn Vec(input: $A, ammount: Int) -> [$A] {
    let list = []: $A
    for i <- 0; i < ammount; i += 1 {
        list.push(input)
    }
    return list
}